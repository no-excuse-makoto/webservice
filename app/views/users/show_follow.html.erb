<div class="main user-show">
  <div class="container">

        <h3><%= @user.name %>の<%= action_name == "following" ? "フォロー" : "フォロワー" %>一覧</h3>

        <ul>
        <% @users.each do |user| %>
        <li>
      <%= link_to user.name, user_path(user) %>
        </li>
        <% end %>
        </ul>
    </div>
</div>